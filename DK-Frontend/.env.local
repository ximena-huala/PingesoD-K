# Deja la URL base VACIA -> el frontend llama /api/... relativo y Vite lo
# proxea a http://localhost:8080 (ver server.proxy en vite.config.ts).
# Al ser mismo origen, el navegador no hace preflight y CORS deja de existir.
VITE_API_URL=
